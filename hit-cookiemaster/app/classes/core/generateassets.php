<?php

namespace hitcookiemaster\app\classes;
defined('ABSPATH') or die('No Time for Looking for Freedom');

    class generateassets
    {

        private function __construct()
        {

        }

        public static function init()
        {
            /*
                        if ( isset( $_GET['page'] ) && $_GET['page'] == 'hit_overview' ) {
                            wp_register_style( 'am_admin_bootstrap',
                                HITPLUGINLIBARY_ASSETS. 'bootstrap/css/bootstrap.min.css',
                                array(),
                                HITDKLIMBISSMASTER_VERSION,
                                'all' );
                            //wp_enqueue_style( 'adding_admin_bootstrap',,array(),HITSTATUS_VERSION,'all');
                            wp_enqueue_style( 'am_admin_bootstrap' );
                        }

            */
        }


}